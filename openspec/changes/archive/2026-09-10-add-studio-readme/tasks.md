## 1. Root README

- [x] 1.1 Add a Russian `README.md` at the repository root that tells someone with Android Studio to open the project, pick an emulator on API 26+, Run, add the **Статус доступа** widget from the home-screen picker, and tap it to check, and verify the file exists and those steps are present
- [x] 1.2 State in that README that the launcher icon only explains how to add the widget and that emulator probes use the host computer's network, and verify both notes are in the file
- [x] 1.3 Keep the README to the Studio emulator path only, and verify it does not document CLI (`gradlew` / `adb`), a physical device, or installing Android Studio from scratch
